// app/about/layout.tsx
import React from 'react';

const AccountPayablesLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default AccountPayablesLayout;