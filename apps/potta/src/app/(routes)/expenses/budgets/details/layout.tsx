// app/about/layout.tsx
import React from 'react';

const DetailBudgetLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>

            {children}
        </>
    );
};
export default DetailBudgetLayout;